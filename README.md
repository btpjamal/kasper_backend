                     NOSSA PLATAFORMA

Cadastro ───────► Login ───────► JWT├ GET /api/tokens├ POST /api/tokens├ PATCH /api/tokens/{id}/revoke


                    API DE MACHINE LEARNING

Sistema externo ──► edupredict_sk_... ──►SHA-256

api_tokens ──► (existe? / ativo? / não expirou?)

ROLE_API ──► POST /api/v1/predictions ──► Modelo de ML
